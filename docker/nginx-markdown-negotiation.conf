# Optional include for host nginx (rsync static deploy): enable Markdown for Agents
# negotiation by running the converter and merging these directives into your
# server { } that serves the Hugo public/ tree.
#
#   map $http_accept $markdown_pref { default 0; "~*text/markdown" 1; }
#   map $uri $markdown_static_asset {
#       default 0;
#       ~*\.(xml|css|js|mjs|png|jpg|jpeg|gif|svg|webp|avif|ico|woff2?|ttf|eot|json|txt|pdf|map|webmanifest|atom)$ 1;
#   }
#   map $uri $markdown_non_html_path {
#       default 0;
#       ~*^/\.well-known/ 1;
#       ~*^/(robots\.txt|llms\.txt|humans\.txt)$ 1;
#   }
#   map "$markdown_pref:$markdown_static_asset:$markdown_non_html_path" $markdown_route { default 0; "1:0:0" 1; }
#
# In location / (before try_files):
#   if ($markdown_route = 1) {
#       rewrite ^ @markdown_preference last;
#   }
#   try_files $uri $uri/ =404;
#
# location @markdown_preference {
#     proxy_pass http://127.0.0.1:3000$request_uri;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
# }
#
# Start the service (example):
#   HTML_ROOT=/var/www/jhell.imipolex.biz/html NODE_ENV=production node /path/to/repo/docker/markdown-server/server.mjs
#
# Requires Node 18+ and: npm install --omit=dev in docker/markdown-server/
